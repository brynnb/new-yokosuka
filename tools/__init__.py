"""Offline extraction and research tools.

Run Python commands from the repository root with ``python3 -m tools.<domain>.<tool>``.
Explicit package imports keep shared helpers usable across tool domains and tests.
"""
